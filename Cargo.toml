[package]
name = "postings"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
license = "MIT OR Apache-2.0"
authors = ["Arc <attobop@gmail.com>"]
description = "Inverted-index postings lists with segment-style updates."

[dependencies]
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"], optional = true }
durability = { path = "../durability", optional = true }
postcard = { version = "1.0", optional = true }

[features]
default = []
serde = ["dep:serde"]
persistence = ["dep:durability", "dep:postcard", "serde"]

[dev-dependencies]
proptest = "1.9"
durability = { path = "../durability" }
serde = { version = "1.0", features = ["derive"] }
tempfile = "3.10"

